type vendor_displayfeature_sysfs, sysfs_type, fs_type;

allow mtk_hal_pq vendor_displayfeature_sysfs:file rw_file_perms;

# HOS2 PQ creates its MML scaler workers only when this property reads as 1.
get_prop(mtk_hal_pq, vendor_mtk_mml_support_prop)
